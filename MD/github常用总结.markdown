#github 常用总结
    git branch
    git checkout 
    git pull 
    git push
    git commit
    git diff
    git fetch upstream 
    git merge upstream/master or git rebase master
    git pull upstream master
